const arr = [1,2];
arr.reduce((acc,curr)=>{
    console.log(acc);  // 1
    console.log(curr); // 2
});

// if initial value paas nahi ki h to acc first value ko and curr secound value ko 
// point karta h.


