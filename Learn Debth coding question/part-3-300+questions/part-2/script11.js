// is code ko optimezed kero.

const data = new Array(100);

for(let i=0;i<data.length;i++){

}

// opttimized karne ke liye 
// ye code har 1 iteration par data.length ko access kar raha h. wich is expensive operation.

// app data.length ko alag se store kara lo.



/*
    let len = data.length;
    for(let i=0;i<len;i++){

    }
*/
