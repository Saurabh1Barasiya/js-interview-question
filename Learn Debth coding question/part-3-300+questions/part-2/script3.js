function init(x,y,z){}

function end(a,b=0,c){}

console.log(init.length);   // 3
console.log(end.length);   // 1

// function ke parament hi us function ke length hoti h.
// agar default parameter laga hua h to count nahi hoga.
// default parameter ke baad ke parameter bhi count nahi hoge.